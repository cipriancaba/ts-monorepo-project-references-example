export enum DummyShared {
  chair,
  table,
}

export const SHARED_CONFIG = {
  DUMMY: true,
  SHARED: 'of course',
  MONO: 'REPO',
};

export function sum(a: number, b: number): number {
  return a + b;
}
